import re
## Данные
page = '''
<!DOCTYPE html>
<html>
<body>
<h1>My Programming Links</h1>
<a href="https://app.finxter.com/">test your Python skills</a>
<a href="https://blog.finxter.com/recursion/">Learn recursion</a>
<a href="https://nostarch.com/">Great books from NoStarchPress</a>
<a href="http://finxter.com/">Solve more Python puzzles</a>
</body>
</html>
'''
## Однострочник
practice_tests = re.findall("(<a.*?finxter.*?(test|puzzle).*?>)", page)# После открывающего тега мы ищем произвольное количество символов («нежадным» образом, чтобы регулярное выражение не захватывало несколько тегов HTML), за которым следует строка символов 'finxter'. Далее ищем произвольное количество символов («нежадным» образом), за которым следует одно вхождение строки символов 'test' либо строки символов 'puzzle'. И снова ищем произвольное количество символов («нежадным»образом), за которым следует закрывающий тег.
## Результат
print(practice_tests)

#операторы «звездочка» '.*?', чтобы всегда искать лишь минимальные со- ответствующие строки символов, вместо, например, очень длинной строки символов, заключенной во множество вложенных тегов.

